import { MoveLeftIonicDirective } from './move-left-ionic.directive';

describe('MoveLeftIonicDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveLeftIonicDirective();
    expect(directive).toBeTruthy();
  });
});
